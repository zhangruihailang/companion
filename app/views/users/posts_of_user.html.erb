<div class="am-tab-panel am-panel-default">
  <div data-am-widget="titlebar" class="am-titlebar am-titlebar-default" >
  <h2 class="am-titlebar-title" style="font-size:1.6rem;color:#4A779D;">
    <%if @user == current_user%>
    我发布的消息
    <%else%>
    <%=@user.name%>发布的消息
    <%end%>
  </h2>
</div> <br>
  <%= render :partial => 'users/user_messages', 
  :locals => { :pre_url => "/posts_of_user?id=#{@user.id}",:tips => '还没有发布过消息！' } 
  %>
</div>